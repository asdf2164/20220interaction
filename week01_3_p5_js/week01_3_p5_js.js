
function setup() {
    initializeFields();
    createCanvas(500, 500);
}

function draw() {
    background(color(0xFA, 0xCA, 0x94));
    fill(color(0x2E, 0x27, 0x89));
    rect(mouseX, mouseY, 100, 150);
}

function initializeFields() {
}
